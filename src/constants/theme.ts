/**
 * Legacy theme re-exports - import from here for backwards compatibility
 * New code should use the design tokens from @/shared/constants instead
 */

import '@/global.css';

export { Colors, Fonts, Spacing, BottomTabInset, MaxContentWidth, type ThemeColor } from '@/shared/constants/legacy-theme';
